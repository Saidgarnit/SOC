input {
  file {
    path => "/var/log/suricata/eve.json"
    codec => "json"
    start_position => "beginning"
    sincedb_path => "/usr/share/logstash/data/sincedb"
    type => "SuricataIDPS"
  }
  udp { port => 5001 codec => json }
}

filter {

  # ================= DROP SURICATA NOISE =================
  if [type] == "SuricataIDPS" and [event_type] in ["http", "flow", "stats"] {
    drop { }
  }


  # Extract src_ip from different formats
  if [event_data][src_ip] {
    mutate { add_field => { "src_ip" => "%{[event_data][src_ip]}" } }
  } else if [source][ip] {
    mutate { add_field => { "src_ip" => "%{[source][ip]}" } }
  }

  # Default index - use 'replace' if it exists or 'add_field' ONLY if it is empty
  if ![target_index] {
    mutate { add_field => { "target_index" => "soc-logs-enriched" } }
  } 
  # Convert src_ip to string
  if [src_ip] {
    mutate { convert => { "src_ip" => "string" } }
  }

  # ================= GEOIP =================

  if [src_ip] {
    geoip {
      source => "src_ip"
      target => "src_geoip"
      database => "/usr/share/logstash/geoip/GeoLite2-City.mmdb"
      ecs_compatibility => "disabled"
      fields => [
        "city_name",
        "country_name",
        "country_code2",
        "location",
        "region_name",
        "autonomous_system_number",
        "autonomous_system_organization"
      ]
    }
  }

  if [dest_ip] {
    geoip {
      source => "dest_ip"
      target => "dest_geoip"
      database => "/usr/share/logstash/geoip/GeoLite2-City.mmdb"
      ecs_compatibility => "disabled"
      fields => [
        "city_name",
        "country_name",
        "country_code2",
        "location",
        "region_name",
        "autonomous_system_number",
        "autonomous_system_organization"
      ]
    }
  }

  # ================= MISP IOC CACHE LOOKUP =================

  if [src_ip] and [event_type] == "alert" {
    if [src_ip] !~ /^(10\.|172\.(1[6-9]|2[0-9]|3[01])\.|192\.168\.|127\.)/ {
      translate {
        field => "src_ip"
        destination => "misp_ioc"
        dictionary_path => "${MISP_IOC_DICTIONARY:/usr/share/logstash/misp_cache/misp_iocs.json}"
        refresh_interval => 300
        exact => true
        override => true
      }
      if [misp_ioc] {
        mutate {
          add_tag => ["misp_threat_match"]
          replace => { "target_index" => "alerts-soc-threats" }
          add_field => {
            "threat_intel_info" => "%{misp_ioc}"
            "threat_risk_score" => "100"
            "threat_category" => "misp"
          }
        }
      }
    }
  }

  # ================= FIX 2: GEOIP WARNINGS =================

  if [src_geoip] {
    mutate { add_field => { "[@metadata][src_geoip_ok]" => "true" } }
  }

  if "_geoip_lookup_failure" in [tags] {
    mutate {
      add_field => { "geoip_note" => "IP not in GeoLite2 database" }
      remove_tag => ["_geoip_lookup_failure"]
    }
  }

}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "${ELASTIC_USERNAME}"
    password => "${ELASTIC_PASSWORD}"
    index => "%{target_index}"
    ssl_verification_mode => "none"
  }
}
