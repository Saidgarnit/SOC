name: "🔨 Wazuh — Authentication Brute Force"
type: frequency
index: .ds-wazuh-alerts-4.x-*
num_events: 5
timeframe:
  minutes: 2
realert:
  minutes: 30
query_key:
- agent.name
- data.srcip
filter:
- query:
    bool:
      must:
      - match:
          rule.groups: authentication_failed

alert:
- slack
alert_text_type: alert_text_only
alert_text: "\n🔨 *Wazuh — Authentication Brute Force*\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nTime: {0}\nHost: {1} ({2})\nLevel: {3} | Rule: {4}\nDescription: {5}\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nMITRE ATT&CK:\n  Tactic: {6}\n  Technique: {7}\n  ID: {8}\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nAttacker IP: {9}\nTarget User: {10}\nFull Log: {11}\n━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━\nKibana: http://localhost:5601\n"
alert_text_args:
- '@timestamp'
- agent.name
- agent.ip
- rule.level
- rule.id
- rule.description
- rule.mitre.tactic
- rule.mitre.technique
- rule.mitre.id
- data.srcip
- data.dstuser
- full_log

slack_webhook_url: "${SLACK_WEBHOOK_URL}"
slack_username_override: SOC Alerts
slack_icon_emoji: ':hammer:'
slack_msg_color: danger
