name: Suricata IDS Alert
type: frequency
index: soc-logs-enriched-*
ignore_unavailable: true
num_events: 3
timeframe:
  minutes: 2
query_key: alert.signature
filter:
  - term:
      event_type.keyword: "alert"
  - term:
      type.keyword: "SuricataIDPS"
  - bool:
      must_not:
        - terms:
            src_ip:
              - "172.18.0.6"
              - "172.18.0.9"
              - "172.18.0.19"
realert:
  minutes: 10
alert_subject: "🚨 [Suricata] {0} — {1} → {2}"
alert_subject_args:
  - alert.signature
  - src_ip
  - dest_ip
alert_text_type: alert_text_jinja
alert_text: |
  🚨 SURICATA IDS/IPS ALERT
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Signature  : {{ alert.signature | default('N/A') }}
  Sig ID     : {{ alert.signature_id | default('N/A') }}
  Severity   : {{ alert.severity | default('N/A') }}
  Action     : {{ alert.action | default('N/A') }}
  Category   : {{ alert.category | default('N/A') }}
  Matches    : {{ num_hits }} events in 2 min
  Timestamp  : {{ timestamp }}
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Source     : {{ src_ip | default('N/A') }}:{{ src_port | default('N/A') }}
  Dest       : {{ dest_ip | default('N/A') }}:{{ dest_port | default('N/A') }}
  Protocol   : {{ proto | default('N/A') }}
  Interface  : {{ in_iface | default('N/A') }}
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  GeoIP Src  : {{ src_geoip.country_name | default('Internal/Private') }}
  GeoIP Dst  : {{ dest_geoip.country_name | default('Internal/Private') }}
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  Kibana: http://localhost:5601
alert:
  - email
  - slack
email:
  - garnitsaid01@gmail.com
slack_webhook_url: "YOUR_SLACK_WEBHOOK_URL"
slack_username_override: "SOC-Alert-Bot"
slack_emoji_override: ":rotating_light:"
slack_msg_color: "danger"
