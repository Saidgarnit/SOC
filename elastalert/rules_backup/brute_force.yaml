name: Brute Force Attack Detected
type: frequency
index: .ds-wazuh-alerts-4.x-*,.ds-wazuh-alerts-4.x-*
ignore_unavailable: true

num_events: 8
timeframe:
  minutes: 5

query_key: data.srcip

filter:
  - bool:
      should:
        - term:
            rule.groups: "authentication_failed"
        - term:
            rule.groups: "sshd"
        - term:
            rule.groups: "web-accesslog"
      minimum_should_match: 1
realert:
  minutes: 10
alert_subject: ":hammer: [Brute Force] %(agent.name)s -- Rule %(rule.id)s"
alert_text_type: alert_text_only
alert_text: |
  :hammer: BRUTE FORCE ATTACK DETECTED
  ________________________________
  Rule       : %(rule.id)s -- %(rule.description)s
  Agent      : %(agent.name)s
  Level      : %(rule.level)s
  Timestamp  : %(@timestamp)s
  Source IP  : %(data.srcip)s
  ________________________________
  Kibana: http://localhost:5601

alert:
  - slack
slack_webhook_url: "https://hooks.slack.com/services/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"
slack_username_override: "SOC-Alert-Bot"
slack_emoji_override: ":hammer:"
slack_msg_color: "danger"
